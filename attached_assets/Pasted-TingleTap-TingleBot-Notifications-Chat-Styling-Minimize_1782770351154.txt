TingleTap – TingleBot Notifications, Chat Styling & Minimized Conversations Upgrade

Implement these fixes without breaking any existing functionality.

1. TingleBot Strip Notifications

Fix notification visibility so User Joined, User Left, Kicked, Unkicked, Banned, Unbanned, Muted, Unmuted, Promoted, Demoted, Announcements, Community Rules, and all future TingleBot notifications are visible to Owner, Admin, Moderator, Badge Holders, Registered Users, and Guests (Purush, Stree, Navrang). Do not restrict by role or badge. Notifications must remain visible for every client until the configured auto-delete timer expires. Fix the issue where Registered Users see notifications for only a few milliseconds while the Owner sees them correctly. Use the same synchronization, rendering, and auto-delete lifecycle for every client.

2. Chat Typography & Style Synchronization

Default styling for Guests (Purush, Stree, Navrang) and Registered Users:

- Username: 12px Bold
- Message: 11px Normal
- Default font

Every viewer must see identical typography regardless of their own role.

If a Registered User changes their message color, every connected client must instantly see the same color.

If a Badge Holder changes their username color and/or message color, those styles must synchronize correctly for Owner, Admin, Moderator, Badge Holders, Registered Users, and Guests.

The sender's saved style is the single source of truth. The viewer's role, badge, theme, or permissions must never override another user's typography or colors.

3. Minimized Conversations Redesign

Completely redesign Minimized Conversations with a premium luxury UI featuring modern SVG icons, elegant glassmorphism, rounded corners, smooth animations, compact layout, hover effects, unread badges, online indicators, and full mobile responsiveness.

Integrate it directly with LuxuryPrivateMessage.

- When a user clicks Minimize in a private message window, that conversation must instantly appear in Minimized Conversations.
- Each minimized conversation must display the user's Display Picture (DP), Display Name, online/offline status, unread count, and latest message preview.
- Support multiple minimized conversations with proper ordering by latest activity.
- Clicking a minimized conversation must reopen the same LuxuryPrivateMessage window with the complete chat history, draft text, attachments, scroll position, and conversation state preserved.
- Unread counts must continue updating in real time while minimized.
- Prevent duplicate minimized entries, lost conversations, state resets, or UI glitches.
- All minimize/restore actions must stay synchronized across the application.

Perform a complete regression test to ensure notifications, typography, color synchronization, private messaging, minimized conversations, permissions, themes, and TingleBot remain fully synchronized and functional across all clients.